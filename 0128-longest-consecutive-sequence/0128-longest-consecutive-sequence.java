class Solution {
    public int longestConsecutive(int[] nums) {
        Hashset<Integer> map = new Hashmap<>();
        for(int i = 0 ; i < nums.length ; i++){
            map.put(nums[i] );
        }
        for(int i: nums  ){
            if(map.get(i))
        }
    }
}